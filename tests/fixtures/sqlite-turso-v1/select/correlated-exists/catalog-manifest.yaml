version: 1
resources:
  projects:
    source: { table: projects }
    columns: { id: { type: string }, tenant_id: { type: string } }
  tasks:
    source: { table: tasks }
    columns: { id: { type: string }, project_id: { type: string }, tenant_id: { type: string } }

version: 1
resources:
  projects:
    roles:
      member:
        select:
          columns: [id, tenant_id]
          filter: { tenant_id: { eq: { session: tenant_id } } }
          allow_windows: true

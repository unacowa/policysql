version: 1
resources:
  projects:
    source:
      table: projects
    columns:
      id: { type: string }
      tenant_id: { type: string }
      name: { type: string }
      status: { type: string }
      created_by: { type: string }

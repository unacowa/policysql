resource: comments

roles:
  author:
    insert:
      columns:
        - body
        - name: private_note
          visible_if:
            author_id:
              eq:
                session: subject_id
          on_deny: null

# Open questions

These choices are intentionally not finalized.

1. Which SQLite parser implementation best satisfies the accepted grammar, source-location, AST-stability, fuzzing, and differential-test requirements?
2. Should policy metadata remain YAML/JSON, or support a typed configuration language in a future API version?
3. Should commands or business procedures be separate versioned HTTP resources or a later extension?

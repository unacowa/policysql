resource: authors
roles:
  member:
    select:
      columns: [id, name]
      filter: { tenant_id: { eq: { session: tenant_id } } }
      limit: 100
      allow_aggregations: false
      allow_windows: false
